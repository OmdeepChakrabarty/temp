{
  "name": "ai-native-three-animation-harness",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.0.0 <25"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test:unit": "tsc --outDir .test-dist --noEmit false && node --test tests/**/*.test.mjs"
  },
  "devDependencies": {
    "@types/three": "^0.186.0",
    "ajv": "^8.20.0",
    "three": "^0.186.0",
    "typescript": "^7.0.2"
  }
}
